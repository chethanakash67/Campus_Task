# server/.env - EXAMPLE CONFIGURATION

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=campustasks
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here

# JWT Secret (change this to a random string in production)
JWT_SECRET=your_jwt_secret_key_change_this_in_production

# Server Port
PORT=5001

# Email Configuration (for password reset and welcome emails)
# For Gmail:
# 1. Go to Google Account settings
# 2. Enable 2-Factor Authentication
# 3. Generate an "App Password" for nodemailer
# 4. Use that app password below (not your regular Gmail password)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here

# Example with Gmail App Password:
# EMAIL_USER=campustasks@gmail.com
# EMAIL_PASSWORD=abcd efgh ijkl mnop (16-character app password without spaces)

# For other email providers, you may need to configure nodemailer differently
# See: https://nodemailer.com/smtp/